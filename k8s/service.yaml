apiVersion: v1
kind: Service
metadata:
  name: monpfe-app
  namespace: default
spec:
  type: NodePort
  selector:
    app: monpfe
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30000
